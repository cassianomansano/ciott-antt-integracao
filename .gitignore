__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
venv/

# Banco SQLite local (operacoes do cliente)
*.db
*.sqlite
*.sqlite3

# Logs HTTP (contem payloads com dados do cliente)
*.log

# Config persistido (cert path, senha, CNPJ, RNTRC)
.tester_config.json

# Certificados — NUNCA commitar
*.pfx
*.p12
*.pem
*.key
*.cer
*.crt

# Runtime config gerado dinamicamente pela pythonnet (criado em runtime)
**/GeradorCIOTShared.runtimeconfig.json

# IDE / OS / Agents
.idea/
.vscode/
.claude/
.cursor/
*.swp
.DS_Store
Thumbs.db
.pytest_cache/

# .env real (.env.example pode commitar)
.env


# Projeto C# externo (referencia, nao redistribuir)
.docs/projetoTesteCIOT.zip

# Dump cru do PDF DCS (extracao pdfminer, bagunçado). A versao curada e
# util esta versionada em .docs/dcs_regras.md
.docs/dcs_pef_v11.txt

# PDB debug symbols (gigantes, nao necessarios pra rodar)
*.pdb
!.docs/geradorCIOTDLL/GeradorCIOTShared.pdb
